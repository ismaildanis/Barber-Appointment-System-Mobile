import { ThemedText } from "@/components/themed-text";
import { ThemedView } from "@/components/themed-view";
export default function BarberCalendar() {
    return (
        <ThemedView>
            <ThemedText>BarberCalendar</ThemedText>
        </ThemedView>
    );
}